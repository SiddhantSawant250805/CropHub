# Handle line endings automatically for files detected as text 
# and leave binary files (like images) alone.
* text=auto

# Explicitly declare text files you want to always be LF 
# (Good for shell scripts and Supabase migrations)
*.sh text eol=lf
*.sql text eol=lf

# Denote files that are truly binary and should not be modified
*.png binary
*.jpg binary